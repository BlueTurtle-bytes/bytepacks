apiVersion: tekton.dev/v1
kind: ClusterTask
metadata:
  name: apexpack-self-build
  labels:
    app.kubernetes.io/version: "1.0"
  annotations:
    tekton.dev/displayName: "apexpack self-build"
    tekton.dev/description: >-
      Bootstrappable self-build for the apexpack project. No pre-existing apexpack
      image required. Step 1 compiles the Go binary. Step 2 installs melange, apko,
      and bubblewrap into a Wolfi base image and runs the compiled binary to produce
      the OCI tarball. Step 3 pushes with crane.
spec:
  params:
    - name: IMAGE
      description: >-
        Full OCI image reference including tag
        (e.g. artifacts.corp.com/apexpack/apexpack:v1.2.3).
    - name: VERSION
      description: "APK package version — typically $(tasks.version-release.results.release-version)."
      default: "0.0.1"
    - name: GO_IMAGE
      description: "Go SDK image used to compile the apexpack binary."
      default: "golang:1.23-alpine"
    - name: WOLFI_IMAGE
      description: >-
        Wolfi base image used to install melange, apko, and bubblewrap for the build step.
        Override to pull from your Artifactory mirror.
      default: "cgr.dev/chainguard/wolfi-base:latest"
    - name: additional-tags
      description: "Extra tags to stamp after push (e.g. latest). Empty = primary tag only."
      type: array
      default: []
    - name: image-base
      description: "Image path without tag, used when stamping additional-tags."
      default: ""
    - name: TLS_CA_PATH
      description: "Absolute path to a CA certificate (PEM). Set when traffic is routed through a TLS-intercepting proxy."
      default: ""

  workspaces:
    - name: source
      description: "Cloned apexpack source (maps to corporate 'source' workspace)."
    - name: shared
      description: "Shared workspace — compiled binary and OCI tarball are written here."

  volumes:
    - name: docker-credentials
      secret:
        secretName: regcred
        items:
          - key: .dockerconfigjson
            path: config.json

  results:
    - name: IMAGE_TARBALL
      description: "Path to the OCI image tarball."
    - name: pushed-image
      description: "The primary image reference that was pushed."

  steps:
    # ── Step 1: Compile Go binary ─────────────────────────────────────────────
    # CGO_ENABLED=0 produces a fully static binary — runs in any Linux container
    # without needing glibc, so it works inside the Wolfi step that follows.
    - name: compile
      image: $(params.GO_IMAGE)
      workingDir: $(workspaces.source.path)
      env:
        - name: CGO_ENABLED
          value: "0"
        - name: GOFLAGS
          value: "-trimpath"
        - name: GOCACHE
          value: "$(workspaces.shared.path)/.gocache"
        - name: GOMODCACHE
          value: "$(workspaces.shared.path)/.gomodcache"
      script: |
        #!/bin/sh
        set -e
        go build -o $(workspaces.shared.path)/apexpack ./cmd/apexpack
        echo "Binary: $(du -sh $(workspaces.shared.path)/apexpack | cut -f1)"

    # ── Step 2: Build OCI image ───────────────────────────────────────────────
    # Uses a plain Wolfi base — no apexpack image needed. Installs melange, apko,
    # and bubblewrap via apk, then runs the binary compiled in step 1.
    - name: build-image
      image: $(params.WOLFI_IMAGE)
      securityContext:
        privileged: true
        runAsUser: 0
      workingDir: $(workspaces.source.path)
      script: |
        #!/bin/sh
        set -e

        apk add --no-cache melange apko bubblewrap git ca-certificates

        TLS_CA_FLAG=""
        if [ -n "$(params.TLS_CA_PATH)" ]; then
          cat "$(params.TLS_CA_PATH)" >> /etc/ssl/certs/ca-certificates.crt
          TLS_CA_FLAG="--tls-extra-ca $(params.TLS_CA_PATH)"
        fi

        $(workspaces.shared.path)/apexpack build . \
          --tag $(params.IMAGE) \
          --version $(params.VERSION) \
          --output $(workspaces.shared.path)/output \
          --project-name apexpack \
          $TLS_CA_FLAG

        printf "%s" "$(workspaces.shared.path)/output/apexpack.tar" > $(results.IMAGE_TARBALL.path)
        ls -lh $(workspaces.shared.path)/output/

    # ── Step 3: Push to registry ──────────────────────────────────────────────
    - name: push
      image: gcr.io/go-containerregistry/crane:debug
      env:
        - name: DOCKER_CONFIG
          value: /etc/docker-creds
      volumeMounts:
        - name: docker-credentials
          mountPath: /etc/docker-creds
          readOnly: true
      script: |
        #!/bin/sh
        set -e
        TARBALL=$(cat $(results.IMAGE_TARBALL.path))
        crane push "$TARBALL" "$(params.IMAGE)"
        printf "%s" "$(params.IMAGE)" > $(results.pushed-image.path)
        echo "Pushed: $(params.IMAGE)"

    # ── Step 4: Stamp additional tags (e.g. latest) ───────────────────────────
    - name: tag-additional
      image: gcr.io/go-containerregistry/crane:debug
      env:
        - name: DOCKER_CONFIG
          value: /etc/docker-creds
      volumeMounts:
        - name: docker-credentials
          mountPath: /etc/docker-creds
          readOnly: true
      script: |
        #!/bin/sh
        set -e
        BASE="$(params.image-base)"
        if [ -z "$BASE" ]; then exit 0; fi
        for TAG in "$@"; do
          [ -z "$TAG" ] && continue
          echo "Tagging ${BASE}:${TAG}"
          crane tag "$(params.IMAGE)" "${BASE}:${TAG}"
        done
      args:
        - $(params.additional-tags[*])
